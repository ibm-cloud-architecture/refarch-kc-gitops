apiVersion: apps/v1
kind: Deployment
metadata:
  name: voyages-ms
spec:
  template:
    spec:
      containers:
      - name: voyagesms
        env:
          - name: KAFKA_APIKEY
            valueFrom:
              secretKeyRef:
                name: "eventstreams-apikey"
                key: binding
