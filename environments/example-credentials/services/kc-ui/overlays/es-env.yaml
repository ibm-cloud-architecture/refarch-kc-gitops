apiVersion: apps/v1
kind: Deployment
metadata:
  name: kc-ui
spec:
  template:
    spec:
      containers:
      - name: kc-ui
        env:
          - name: KAFKA_APIKEY
            valueFrom:
              secretKeyRef:
                name: "eventstreams-apikey"
                key: binding
